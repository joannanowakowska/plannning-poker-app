# Acceptance scenarios

```gherkin
Feature: <feature name>

Scenario: <happy path>
  Given <precondition>
  When <action>
  Then <exact observable outcome>
```
Verify: `<literal command an agent runs; expected result>`

<!-- APPROVED BY DEVELOPER: no -->
